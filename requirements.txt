# Runtime dependencies for INVISABLE Guardian core.
# (Security tools — CodeQL, Semgrep, Gitleaks, Trivy, ZAP, etc. — are installed
#  separately via the docker-compose stack and/or CI, not via pip.)
pyyaml>=6.0
jsonschema>=4.20
pydantic>=2.5
click>=8.1
rich>=13.7
fastapi>=0.110
uvicorn>=0.27
jinja2>=3.1
