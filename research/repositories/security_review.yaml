# research/repositories/security_review.yaml
# GENERATED by scratchpad/gen_catalogue.py — do not edit by hand; regenerate.
#
# LIMITATION: GitHub API / network access was not available when this was produced.
# Quantitative metadata (default_branch, evaluated_commit, latest_release, archived,
# exact licence, dependency/model licences, maintenance signals) is therefore marked
# `pending_live_discovery: true` with a reproducible discovery command. Per the
# platform charter, metadata is never fabricated. The DECISION fields are architectural
# judgements made from each project's category and purpose, and are actionable now.

schema_version: 1
quarantine_procedure:
  - "metadata-first; no cloning during discovery."
  - "no install scripts, git hooks, submodules, containers, binaries, Makefiles, tests, or builds until quarantine review passes."
  - "any later execution only in an ephemeral sandbox (isolation/)."
  - "pin to reviewed releases, commits, checksums, container digests."
category_controls:
  A_agentic_security: { isolation_tier: ephemeral_sandbox, default_decision: reference }
  B_agent_governance_mcp: { isolation_tier: n/a, default_decision: benchmark }
  C_ai_model_eval: { isolation_tier: ephemeral_worker, default_decision: adapt }
  D_sast_sca_dast: { isolation_tier: ephemeral_worker_egress_controlled, default_decision: adapt }
  E_runtime_detection: { isolation_tier: isolated_service, default_decision: adapt }
  F_vulnmgmt_aspm_siem_soar_ti: { isolation_tier: isolated_service, default_decision: federate }
  G_policy_identity_secrets_privacy: { isolation_tier: n/a, default_decision: reference }
  H_supplychain_signing_provenance: { isolation_tier: n/a, default_decision: adopt }
  I_sandbox_isolation: { isolation_tier: self, default_decision: adopt }
  J_orchestration_gitops: { isolation_tier: n/a, default_decision: reference }
  K_telemetry_observability_audit: { isolation_tier: n/a, default_decision: adopt }
  L_standards_formats_sim: { isolation_tier: n/a, default_decision: adopt }
  M_curated_discovery: { isolation_tier: n/a, default_decision: reference }
high_risk_flags:
  - "category A offensive agents: reject/reference; never enable attack workflows."
  - "mitre/caldera, openvas, kube-hunter, subfinder, nuclei: active/offensive — scope-constrained, authorised targets only, isolated tier."
